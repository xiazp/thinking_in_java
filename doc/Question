1.eclipse 如何编译jar包
A：java的源码是实时编译的，只有不报错误，直接选择File->export->java->jar file
导出jar包即可。

2. 一个java文件中定义了一个类，还可以在其他文件再定义这个类吗？
A： no

3. this的含义
A: 在成员函数中，this表示调用该函数的对象。

4. for(A a: s){ a = xx;} 这种写法a提示没有引用，有语法错误，为什么？

5. static 方法能被覆盖吗？

6. final、 static 可以修饰 类、对象、成员吗？

7. 什么时候用接口 ，什么时候用抽象类？
A: 接口和抽象类的不同在于： 抽象类提供了部分接口，而接口定义了一个完全抽象的类。
